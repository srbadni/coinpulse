import Link from 'next/link';import { SlidersHorizontal, ArrowUpDown } from 'lucide-react';import { AppShell } from '@/components/layout/AppShell';import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';import { Card } from '@/components/ui/card';import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@/components/ui/table';import { TokenIdentity } from '@/components/shared/TokenRow';import { PercentageBadge } from '@/components/shared/PercentageBadge';import { tokens } from '@/lib/mock/tokens';import { formatUsd } from '@/lib/format';
export default function TokensPage(){return <AppShell><div className="space-y-6"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-[#9CA3AF]">لیست کامل دارایی‌ها</p><h1 className="text-3xl font-black">همه توکن‌ها</h1></div><div className="flex gap-2"><Input placeholder="جستجوی توکن..." className="w-full md:w-80"/><Button variant="outline" className="rounded-xl border-[#262B36]"><SlidersHorizontal/> فیلتر</Button><Button variant="outline" className="rounded-xl border-[#262B36]"><ArrowUpDown/> مرتب‌سازی</Button></div></div><Card className="overflow-hidden"><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>توکن</TableHead><TableHead>قیمت</TableHead><TableHead>۲۴ساعت</TableHead><TableHead>ارزش بازار</TableHead><TableHead>حجم</TableHead><TableHead>عرضه در گردش</TableHead><TableHead>عملیات</TableHead></TableRow></TableHeader><TableBody>{tokens.map(t=><TableRow key={t.symbol} className="cursor-pointer"><TableCell><Link href={`/tokens/${t.symbol.toLowerCase()}`}><TokenIdentity token={t}/></Link></TableCell><TableCell>{formatUsd(t.price)}</TableCell><TableCell><PercentageBadge value={t.change24h}/></TableCell><TableCell>{t.marketCap}</TableCell><TableCell>{t.volume}</TableCell><TableCell>{t.supply}</TableCell><TableCell><Button asChild size="sm" className="rounded-xl bg-[#6EEB83] text-[#0F1117] hover:bg-[#6EEB83]/90"><Link href={`/tokens/${t.symbol.toLowerCase()}`}>جزئیات</Link></Button></TableCell></TableRow>)}</TableBody></Table></div><div className="flex items-center justify-between border-t border-[#262B36] p-4 text-sm text-[#9CA3AF]"><span>نمایش ۱ تا {tokens.length.toLocaleString('fa-IR')}</span><div className="flex gap-2"><Button variant="outline" className="rounded-xl border-[#262B36]">قبلی</Button><Button variant="outline" className="rounded-xl border-[#262B36]">بعدی</Button></div></div></Card></div></AppShell>}
