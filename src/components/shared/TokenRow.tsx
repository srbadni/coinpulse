import Link from 'next/link';import type { Token } from '@/types';import { TokenAvatar } from './TokenAvatar';import { PercentageBadge } from './PercentageBadge';import { formatUsd } from '@/lib/format';
export function TokenIdentity({token}:{token:Token}){return <div className="flex items-center gap-3"><TokenAvatar symbol={token.symbol} color={token.color}/><div><p className="font-bold text-[#F5F7FA]">{token.persianName}</p><p className="text-xs text-[#9CA3AF]">{token.name} • {token.symbol}</p></div></div>}
export function TokenRow({token}:{token:Token}){return <Link href={`/tokens/${token.symbol.toLowerCase()}`} className="grid grid-cols-[1fr_auto] items-center rounded-xl p-3 hover:bg-white/5"><TokenIdentity token={token}/><div className="text-left"><p className="font-bold">{formatUsd(token.price)}</p><PercentageBadge value={token.change24h}/></div></Link>}
